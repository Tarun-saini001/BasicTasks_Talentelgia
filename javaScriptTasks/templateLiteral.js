const Name = "Tarun";

const message = `Hi  ${Name} , how are you`

console.log(message);

// $ is rarely used in normal sentences
//it does not conflict with other syntaxes so ${} becomes a unique pattern
//it tells js "start evaluating js expression now" 